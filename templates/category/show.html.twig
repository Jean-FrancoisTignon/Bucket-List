{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h2>Categorie</h2>

    <table class="table">
        <tbody>
            <tr>
                <th class="col-wish0">Id</th>
                <td class="col-wish1">Nom</td>
            </tr>
            <tr>
                <th class="col-wish0">{{ category.id }}</th>
                <td class="col-wish1">{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('app_category_index') }}"><button type="button" class="btn btn-modifier">Retour</button></a>




{% endblock %}
