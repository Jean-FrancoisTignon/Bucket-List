{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | {{ wish.title }}
{% endblock %}

{% block body %}
    <h2>{{ wish.title }} [{{ wish.category.name }}]</h2>
    <div>{{ wish.description | nl2br }}</div><br>
    <div class="wish-infos">{{ wish.author }}, le {{ wish.dateCreated | date('d-m-y') | nl2br }}</div>
    <br>
    <div>
        <a href="{{ path('app_wish_list') }}"><button type="button" class="btn btn-modifier">Retour</button></a>
    </div>


{% endblock %}

